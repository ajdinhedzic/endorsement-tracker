spring:
  h2:
    console:
      enabled: true
  profiles:
    active: testing
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
application:
  jwt:
    secret: tempsecret
  sendgrid:
    key: not-needed
    password-reset-template-id: templateid
